[build]
  functions = "netlify/functions"
  publish = "."

[functions]
  node_bundler = "esbuild"
```

5. Clique sur **"Commit changes"** → **"Commit directly to main"** → **"Commit changes"**

---

## ✅ Ton repo doit maintenant ressembler à ça
```
ton-repo/
├── index.html                          ✅
├── netlify.toml                        ✅ (nouveau)
└── netlify/
    └── functions/
        └── create-checkout-session.js  ✅
